"""Shared PySide6 design system for the Mitchel NLP desktop apps.

Both `mitchel_pipeline` and `email_triage_ui` depend on this package; it
depends on neither of them, and the two never import each other. One accent
color, one corner-radius scale, one set of themed widgets, used by both
windows.
"""
